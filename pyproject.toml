[project]
name = "orbbec-camera"
version = "0.1.0"
description = "Viam module for supporting Orbbec 3D cameras"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "viam-sdk==0.45.2",
  "typing-extensions",
  "pillow",
  "opencv-python",
  "numpy<2",
]
